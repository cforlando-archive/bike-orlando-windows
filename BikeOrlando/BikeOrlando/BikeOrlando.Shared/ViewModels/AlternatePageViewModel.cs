using Microsoft.Maps.SpatialToolbox;
using Microsoft.Maps.SpatialToolbox.Bing;
using System;
using System.Collections.Generic;
using System.Text;
using Windows.UI.Xaml;

namespace BikeOrlando.ViewModels
{
    // not really an MVVM viewModel but removes some of the page code behind
    public class AlternatePageViewModel : ViewModelBase
    {
        //public void LoadMap(SpatialDataSet data, MapView mapView, MapLayer pinLayer,MapLayer polyLayer, ShapeStyle shapeStyle, EventHandler<RoutedEventArgs> geometryTappedEvent)
        //{
           
        //   // MapTools.LoadGeometries(data, pinLayer, polyLayer, shapeStyle, geometryTappedEvent);

        //}

    }
}
